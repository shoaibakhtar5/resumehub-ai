<x-admin-layout title="View Role">
    @include('admin.access._tabs')
    <div class="mx-auto max-w-5xl"><div class="mb-5 flex items-center justify-between"><a href="{{ route('admin.roles') }}" class="text-sm font-semibold text-slate-600 hover:text-indigo-600">← Back to Roles</a><a href="{{ route('admin.roles.edit', $role) }}" class="inline-flex items-center gap-2 rounded-lg bg-indigo-600 px-4 py-2 text-sm font-semibold text-white"><x-ui.icon name="pencil-square" class="h-4 w-4" /> Edit Role</a></div>
        <section class="rounded-xl border border-slate-200 bg-white p-6 shadow-sm"><div class="flex flex-wrap items-start justify-between gap-4"><div><div class="flex items-center gap-3"><h2 class="font-display text-2xl font-bold">{{ Str::headline($role->name) }}</h2>@if($role->is_system)<span class="rounded-full bg-amber-50 px-2.5 py-1 text-xs font-semibold text-amber-700">Protected system role</span>@endif</div><p class="mt-2 text-sm text-slate-500">{{ $role->description ?: 'No description provided.' }}</p></div><div class="flex gap-3"><div class="rounded-xl bg-slate-50 px-5 py-3 text-center"><p class="text-xl font-bold">{{ $role->users_count }}</p><p class="text-xs text-slate-500">Users</p></div><div class="rounded-xl bg-indigo-50 px-5 py-3 text-center"><p class="text-xl font-bold text-indigo-700">{{ $role->permissions_count }}</p><p class="text-xs text-indigo-600">Permissions</p></div></div></div></section>
        <div class="mt-5 grid gap-5 lg:grid-cols-2"><section class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><h3 class="font-display font-semibold">Assigned Permissions</h3><div class="mt-4 flex flex-wrap gap-2">@forelse($role->permissions as $permission)<a href="{{ route('admin.permissions.show', $permission) }}" class="rounded-lg border border-indigo-100 bg-indigo-50 px-3 py-2 text-xs font-semibold text-indigo-700 hover:border-indigo-300">{{ $permission->name }}</a>@empty<p class="text-sm text-slate-500">No permissions assigned.</p>@endforelse</div></section><section class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><h3 class="font-display font-semibold">Assigned Users</h3><div class="mt-4 space-y-2">@forelse($role->users as $managedUser)<a href="{{ route('admin.users.show', $managedUser) }}" class="flex items-center gap-3 rounded-lg border border-slate-200 p-3 hover:border-indigo-200"><span class="flex h-8 w-8 items-center justify-center rounded-full bg-indigo-50 text-xs font-bold text-indigo-700">{{ strtoupper(substr($managedUser->name, 0, 1)) }}</span><span><span class="block text-sm font-semibold">{{ $managedUser->name }}</span><span class="text-xs text-slate-500">{{ $managedUser->email }}</span></span></a>@empty<p class="text-sm text-slate-500">No users assigned.</p>@endforelse</div></section></div>
    </div>
</x-admin-layout>
