@php($editing = (bool) $permission)
<x-admin-layout :title="$editing ? 'Edit Permission' : 'Create Permission'">
    @include('admin.access._tabs')
    <div class="mx-auto max-w-3xl"><a href="{{ route('admin.permissions') }}" class="mb-5 inline-flex text-sm font-semibold text-slate-600 hover:text-indigo-600">← Back to Permissions</a><form method="POST" action="{{ $editing ? route('admin.permissions.update', $permission) : route('admin.permissions.store') }}" class="overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm">@csrf @if($editing) @method('PATCH') @endif<div class="border-b border-slate-200 px-6 py-5"><h2 class="font-display text-lg font-semibold">{{ $editing ? 'Edit permission' : 'Create permission' }}</h2><p class="mt-1 text-sm text-slate-500">Use a clear capability name such as users.manage.</p></div><div class="space-y-5 p-6"><label class="block"><span class="mb-1.5 block text-sm font-semibold">Permission Name *</span><input name="name" value="{{ old('name', $permission?->name) }}" required @readonly($permission?->is_system) placeholder="users.manage" class="w-full rounded-lg border-slate-200 font-mono text-sm focus:border-indigo-500 focus:ring-indigo-200">@if($permission?->is_system)<span class="mt-1 block text-xs text-slate-500">System permission names are fixed because policies depend on them.</span>@endif @error('name')<span class="mt-1 block text-xs text-rose-600">{{ $message }}</span>@enderror</label><label class="block"><span class="mb-1.5 block text-sm font-semibold">Description</span><textarea name="description" rows="4" class="w-full rounded-lg border-slate-200 text-sm focus:border-indigo-500 focus:ring-indigo-200">{{ old('description', $permission?->description) }}</textarea>@error('description')<span class="text-xs text-rose-600">{{ $message }}</span>@enderror</label></div><div class="flex justify-end gap-3 border-t border-slate-200 bg-slate-50 px-6 py-4"><a href="{{ route('admin.permissions') }}" class="rounded-lg border border-slate-200 bg-white px-4 py-2 text-sm font-semibold">Cancel</a><button class="rounded-lg bg-indigo-600 px-5 py-2 text-sm font-semibold text-white">{{ $editing ? 'Update Permission' : 'Create Permission' }}</button></div></form></div>
</x-admin-layout>
