<x-admin-layout title="View Permission">
    @include('admin.access._tabs')
    <div class="mx-auto max-w-4xl"><div class="mb-5 flex items-center justify-between"><a href="{{ route('admin.permissions') }}" class="text-sm font-semibold text-slate-600 hover:text-indigo-600">← Back to Permissions</a><a href="{{ route('admin.permissions.edit', $permission) }}" class="inline-flex items-center gap-2 rounded-lg bg-indigo-600 px-4 py-2 text-sm font-semibold text-white"><x-ui.icon name="pencil-square" class="h-4 w-4" /> Edit</a></div><section class="rounded-xl border border-slate-200 bg-white p-6 shadow-sm"><div class="flex flex-wrap items-start justify-between gap-4"><div><div class="flex items-center gap-3"><h2 class="font-mono text-xl font-bold text-indigo-700">{{ $permission->name }}</h2>@if($permission->is_system)<span class="rounded-full bg-amber-50 px-2.5 py-1 text-xs font-semibold text-amber-700">Protected</span>@endif</div><p class="mt-3 text-sm text-slate-500">{{ $permission->description ?: 'No description provided.' }}</p></div><div class="rounded-xl bg-indigo-50 px-5 py-3 text-center"><p class="text-xl font-bold text-indigo-700">{{ $permission->roles_count }}</p><p class="text-xs text-indigo-600">Assigned roles</p></div></div><div class="mt-6 border-t border-slate-100 pt-5"><h3 class="font-display font-semibold">Roles using this permission</h3><div class="mt-3 flex flex-wrap gap-2">@forelse($permission->roles as $role)<a href="{{ route('admin.roles.show', $role) }}" class="rounded-lg border border-slate-200 px-3 py-2 text-sm font-semibold hover:border-indigo-200 hover:text-indigo-600">{{ Str::headline($role->name) }}</a>@empty<p class="text-sm text-slate-500">Not assigned to any role.</p>@endforelse</div></div></section></div>
</x-admin-layout>
